// Main entry point for ELD Progress Report
// Registers the custom element and initializes the app
import App from './App.svelte';

// Register the custom element
// The component itself handles the customElement registration via svelte:options

console.log('[ELD] Progress Report initialized');

export default App;