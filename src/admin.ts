/**
 * Admin page entry point for PS Toolbar.
 * Registers the <ps-toolbar-admin> custom element via svelte:options in AdminPage.svelte.
 */
import AdminPage from './components/AdminPage.svelte';

console.log('[PS Toolbar Admin] initialized');

export default AdminPage;
